<h1>Edit Page</h1>
<%= form_for @page, :url => { :action => 'update' } do |f| %>
<% if @page.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@post.errors.count, "error") %> prohibited
      this page from being saved:</h2>
    <ul>
    <% @page.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <%= f.label :title %>
  <%= f.text_field :title, class: 'field-pad'%></br>
  <%= f.label :content %>
  <%= f.text_area :content,class: 'text-area', size: "80x30", placeholder: 'Insert Her your Text' %></br>
  <%= f.submit "Update" , class: 'btn-space' %>     
<% end %>
<script type="text/javascript">
  $('#page_content').wysihtml5();
</script>
