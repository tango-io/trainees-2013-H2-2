%h3 Discover Projects
%p.subtitle Passion, ideas, and ambition abound. Start exploring!
.row
  .col-lg-9
    %ul
      %h3 Staff Picks
      - @projects_category.each do |project|
        %li
          %a{:href => "#{project_path(project)}"}
            .project-bg
              .project-wrap
                =image_tag "#{project.picture}", class: 'img-project'
                %h5 
                  %strong #{project.name}
                  %span By #{project.author}
                %p #{project.description}
                .progress
                  #{project.period}
  .col-lg-3
    %ul
      %li
        %h3 Categories
      - @categories.each do |category|
        %li
          %h3
            = link_to "#{category.name}", ""
          %ul
          - category.subcategories.all.each do |subcategory|
            %li 
              = link_to subcategory.name, projects_path(:id)
